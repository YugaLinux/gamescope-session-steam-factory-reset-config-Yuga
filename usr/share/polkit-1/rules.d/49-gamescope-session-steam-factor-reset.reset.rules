polkit.addRule(function(action, subject) {
	if (action.id == "org.gamescope-session-steam-system-update.run-steamos-factory-reset-config" && subject.isInGroup("wheel")) {
		return polkit.Result.YES;
	}
});
